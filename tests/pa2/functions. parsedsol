(() () (let add1 = (lambda (x) (+ x 1)) in 
  (let add2 = (lambda (y) (+ y 1)) in
    (let rec fact = (lambda (n) (* n (fact (- n 1)))) in
      (let higherOrder = (lambda (f) (lambda (x) (f (f x)))) in
        (let partiallyApplied = (higherOrder fact) in (partiallyApplied 3)))))))